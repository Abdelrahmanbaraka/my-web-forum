<!DOCTYPE html>
<html lang="<%= currentLanguage %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Web Forum</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/forum.css">
    <link rel="stylesheet" href="/css/post.css">
</head>
<body>
    <div class="layout-container">
        <%- include('partials/header') %>

        <main class="main-content">
            <div class="post-container">
                <!-- Navigation -->
                <div class="post-navigation">
                    <a href="/" class="back-link">
                        ← <%= currentLanguage === 'de' ? 'Zurück zur Übersicht' : 'Back to posts' %>
                    </a>
                </div>

                <!-- Main Post -->
                <article class="single-post">
                    <header class="post-header">
                        <h1 class="post-title"><%= post.title %></h1>
                        <div class="post-meta">
                            <div class="meta-left">
                                <span class="post-category <%= post.category %>">
                                    <%= post.category %>
                                </span>
                                <% if (post.is_anonymous) { %>
                                    <span class="anonymous-badge">🎭 <%= currentLanguage === 'de' ? 'Anonym' : 'Anonymous' %></span>
                                <% } %>
                            </div>
                            <div class="meta-right">
                                <span class="post-author">
                                    <%= currentLanguage === 'de' ? 'Von' : 'By' %>:
                                    <% if (post.is_anonymous) { %>
                                        <span class="anonymous-author">🎭 <%= currentLanguage === 'de' ? 'Anonym' : 'Anonymous' %></span>
                                    <% } else { %>
                                        <strong><%= post.username || 'Unknown' %></strong>
                                    <% } %>
                                </span>
                                <span class="post-date">
                                    <%= new Date(post.created_at).toLocaleDateString(currentLanguage === 'de' ? 'de-DE' : 'en-US', {
                                        year: 'numeric',
                                        month: 'long',
                                        day: 'numeric',
                                        hour: '2-digit',
                                        minute: '2-digit'
                                    }) %>
                                </span>
                            </div>
                        </div>
                    </header>

                    <div class="post-content">
                        <%= post.content %>
                    </div>
                </article>

                <!-- Comments Section -->
                <section class="comments-section">
                    <div class="comments-header">
                        <h2>
                            <%= currentLanguage === 'de' ? 'Kommentare' : 'Comments' %>
                            <span class="comments-count">(<%= comments.length %>)</span>
                        </h2>
                    </div>

                    <!-- Add Comment Form -->
                    <% if (currentUser) { %>
                        <div class="add-comment">
                            <form action="/posts/<%= post.id %>/comments" method="POST" class="comment-form">
                                <div class="form-group">
                                    <textarea name="content" required 
                                              placeholder="<%= currentLanguage === 'de' ? 
                                                  'Schreibe einen Kommentar...' : 
                                                  'Write a comment...' %>"
                                              class="comment-textarea"></textarea>
                                </div>
                                
                                <div class="comment-options">
                                    <label class="checkbox-label">
                                        <input type="checkbox" name="isAnonymous">
                                        <span class="checkmark"></span>
                                        🎭 <%= currentLanguage === 'de' ? 'Anonym kommentieren' : 'Comment anonymously' %>
                                    </label>
                                    
                                    <button type="submit" class="submit-comment-btn">
                                        <%= currentLanguage === 'de' ? 'Kommentar posten' : 'Post Comment' %>
                                    </button>
                                </div>
                            </form>
                        </div>
                    <% } else { %>
                        <div class="login-prompt">
                            <p>
                                <%= currentLanguage === 'de' ? 
                                    'Melde dich an, um einen Kommentar zu schreiben.' : 
                                    'Log in to leave a comment.' %>
                            </p>
                            <a href="/auth/login" class="login-link">
                                <%= currentLanguage === 'de' ? 'Anmelden' : 'Login' %>
                            </a>
                        </div>
                    <% } %>

                    <!-- Comments List -->
                    <div class="comments-list">
                        <% if (comments.length > 0) { %>
                            <% comments.forEach(comment => { %>
                                <div class="comment-card">
                                    <div class="comment-header">
                                        <div class="comment-author">
                                            <% if (comment.is_anonymous) { %>
                                                <span class="anonymous-comment-author">
                                                    🎭 <%= currentLanguage === 'de' ? 'Anonym' : 'Anonymous' %>
                                                </span>
                                            <% } else { %>
                                                <strong><%= comment.username || 'Unknown' %></strong>
                                            <% } %>
                                        </div>
                                        <div class="comment-date">
                                            <%= new Date(comment.created_at).toLocaleDateString(currentLanguage === 'de' ? 'de-DE' : 'en-US', {
                                                year: 'numeric',
                                                month: 'short',
                                                day: 'numeric',
                                                hour: '2-digit',
                                                minute: '2-digit'
                                            }) %>
                                        </div>
                                    </div>
                                    <div class="comment-content">
                                        <%= comment.content %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="no-comments">
                                <p>
                                    <%= currentLanguage === 'de' ? 
                                        'Noch keine Kommentare. Sei der Erste, der einen Kommentar schreibt!' : 
                                        'No comments yet. Be the first to comment!' %>
                                </p>
                            </div>
                        <% } %>
                    </div>
                </section>
            </div>
        </main>

        <%- include('partials/footer') %>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>