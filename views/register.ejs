<!DOCTYPE html>
<html lang="<%= currentLanguage %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= currentLanguage === 'de' ? 'Registrieren' : 'Register' %> - Web Forum</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="layout-container">
        <%- include('partials/header') %>

        <main class="main-content">
            <div class="auth-container">
                <div class="auth-card">
                    <div class="auth-header">
                        <h1><%= currentLanguage === 'de' ? 'Registrieren' : 'Register' %></h1>
                        <p><%= currentLanguage === 'de' ? 
                            'Erstelle dein Konto' : 
                            'Create your account' %></p>
                    </div>

                    <% if (error) { %>
                        <div class="error-message">
                            <%= error %>
                        </div>
                    <% } %>

                    <form action="/auth/register" method="POST" class="auth-form">
                        <div class="form-group">
                            <label for="username" class="form-label">
                                <%= currentLanguage === 'de' ? 'Benutzername' : 'Username' %>
                            </label>
                            <input type="text" id="username" name="username" required 
                                   class="form-input" placeholder="<%= currentLanguage === 'de' ? 'Dein Name' : 'Your name' %>">
                        </div>

                        <div class="form-group">
                            <label for="email" class="form-label">
                                <%= currentLanguage === 'de' ? 'E-Mail' : 'Email' %>
                            </label>
                            <input type="email" id="email" name="email" required 
                                   class="form-input" placeholder="your@email.com">
                        </div>

                        <div class="form-group">
                            <label for="password" class="form-label">
                                <%= currentLanguage === 'de' ? 'Passwort' : 'Password' %>
                            </label>
                            <input type="password" id="password" name="password" required 
                                   class="form-input" placeholder="••••••••"
                                   minlength="6">
                            <small class="form-help">
                                <%= currentLanguage === 'de' ? 
                                    'Mindestens 6 Zeichen' : 
                                    'At least 6 characters' %>
                            </small>
                        </div>

                        <div class="form-group">
                            <label for="confirmPassword" class="form-label">
                                <%= currentLanguage === 'de' ? 'Passwort bestätigen' : 'Confirm Password' %>
                            </label>
                            <input type="password" id="confirmPassword" name="confirmPassword" required 
                                   class="form-input" placeholder="••••••••">
                        </div>

                        <button type="submit" class="form-submit">
                            <%= currentLanguage === 'de' ? 'Konto erstellen' : 'Create Account' %>
                        </button>
                    </form>

                    <div class="auth-divider">
                        <span><%= currentLanguage === 'de' ? 'oder' : 'or' %></span>
                    </div>

                    <!-- Anonymous Registration -->
                    <form action="/auth/anonymous-login" method="POST" class="anonymous-form">
                        <button type="submit" class="anonymous-btn">
                            🎭 <%= currentLanguage === 'de' ? 'Anonym fortfahren' : 'Continue Anonymously' %>
                        </button>
                        <p class="anonymous-note">
                            <%= currentLanguage === 'de' ? 
                                'Keine E-Mail oder Passwort benötigt' : 
                                'No email or password required' %>
                        </p>
                    </form>

                    <div class="auth-footer">
                        <p>
                            <%= currentLanguage === 'de' ? 
                                'Bereits ein Konto?' : 
                                'Already have an account?' %>
                            <a href="/auth/login" class="auth-link">
                                <%= currentLanguage === 'de' ? 'Jetzt anmelden' : 'Login now' %>
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>
    </div>
</body>
</html>